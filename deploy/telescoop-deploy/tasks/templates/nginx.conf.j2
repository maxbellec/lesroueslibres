{{ ansible_managed | comment }}

{% if maintenance_mode is not defined or not maintenance_mode %}
    {% if has_backend is defined and has_backend %}
    upstream app_servers_{{ project_slug }} {
    server 127.0.0.1:{{ backend_application_port }};
    }
    {% endif %}

    {% if frontend_mode == "SSR" -%}
    upstream app_frontend_{{ project_slug }} {
    server 127.0.0.1:{{ frontend_application_port }};
    }
    {% endif %}
{% endif %}

server {

    listen 443 ssl;

    ssl_certificate /etc/letsencrypt/live/{{ main_hostname }}/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/{{ main_hostname }}/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

    server_name {{ " ".join(all_hostnames) }};

    access_log /var/log/nginx/access_{{ main_hostname.replace('.', '_') }}.log;
    error_log /var/log/nginx/error_{{ main_hostname.replace('.', '_') }}.log;

    {% for hostname in redirect_hostnames -%}
    if ($host = {{ hostname }}) {
        return 301 https://{{ main_hostname }}$request_uri;
    } # managed by Certbot
    {% endfor %}

    {% if use_password %}
    auth_basic "Acces restreint";
    auth_basic_user_file "{{ passwd_dir }}/passwdfile";
    {% endif %}

    # http://stackoverflow.com/questions/15238506/djangos-suspiciousoperation-invalid-http-host-header
    # Deny illegal Host headers
    if ($host !~* ^({{ "|".join(all_hostnames) }})$ ) {
        return 444;
    }

    {% if maintenance_mode is defined and maintenance_mode %}
    # Serve static assets (like logo)
    location ~ \.(png|jpg|jpeg|gif|svg|ico)$ {
        root {{ static_path }};
        try_files $uri =404;
    }

    # Serve maintenance page for all requests
    location / {
        root {{ static_path }};
        try_files /{{ maintenance_page | default('maintenance.html') }} =503;
    }
    {% else %} {# no maintenance undergoing #}
    # gzip compression
    gzip on;
    gzip_vary on;
    gzip_types application/json application/javascript application/xml text/css text/javascript text/plain text/xml image/svg+xml;

    {% if has_backend is defined and has_backend %}
    # backend static stuff
    location /static  {
        autoindex off;
        alias {{ backend_static_path }};
    }

    {% if backend_media_path is defined %}
    location /media  {
        autoindex off;
        alias {{ backend_media_path }};
    }
    {% endif %}

    proxy_read_timeout {{ requests_timeout }};
    proxy_connect_timeout {{ requests_timeout }};
    proxy_send_timeout {{ requests_timeout }};
    send_timeout {{ requests_timeout }};


    {% for uri in server_uris_passed_to_backend -%}
    location /{{ uri }}/ {
        proxy_pass         http://app_servers_{{ project_slug }};
        proxy_redirect     off;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Host $server_name;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
    {% endfor %}
    {% endif %}

    # send all the rest to the frontend
    {% if frontend_mode is none or frontend_mode == "static" -%}
    location / {
        try_files $uri $uri/ $uri.html /index.html =404;
        root {{ static_path }};

        # Cache static assets
        location ~* \.(png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            access_log off;
        }

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "no-referrer-when-downgrade" always;
    }
    {% elif frontend_mode == "SSR" -%}
    location / {
        proxy_pass         http://app_frontend_{{ project_slug }}/;
        proxy_redirect     off;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Host $server_name;
    }
    {% endif %} {# frontend mode #}
    {% endif %} {# maintenance mode #}

}

server {
    {% for hostname in public_hostnames -%}
    if ($host = {{ hostname }}) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    {% endfor %}

    {% for hostname in redirect_hostnames -%}
    if ($host = {{ hostname }}) {
        return 301 https://{{ main_hostname }}$request_uri;
    } # managed by Certbot
    {% endfor %}

    server_name {{ " ".join(all_hostnames) }};

    listen 80;
    return 404; # managed by Certbot
}
